<UserControl
    x:Class="TimelineApp.ZoomableTimeline"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TimelineApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="BlockRectangleStyle" TargetType="Rectangle">
            <Setter Property="Height" Value="24" />
            <Setter Property="Stroke" Value="Red" />
            <Setter Property="StrokeThickness" Value="1" />
            <Setter Property="Fill" Value="Red" />
        </Style>
        <DataTemplate x:Key="BlockTemplate">
            <Grid>
                <Rectangle Style="{StaticResource BlockRectangleStyle}" />
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer
            x:Name="PART_Scroll"
            HorizontalScrollBarVisibility="Visible"
            VerticalScrollBarVisibility="Disabled">
            <StackPanel
                HorizontalAlignment="Left"
                Orientation="Vertical"
                PreviewMouseDown="OnPreviewMouseDown"
                PreviewMouseMove="OnPreviewMouseMove"
                PreviewMouseUp="OnPreviewMouseUp"
                PreviewMouseWheel="OnPreviewMouseWheel">
                <Canvas
                    x:Name="TicksCanvas"
                    Height="28"
                    Background="White" />
                <Canvas
                    x:Name="BlocksSurface"
                    Height="120"
                    Background="LightGray">
                    <ItemsControl
                        x:Name="PART_Items"
                        ItemTemplate="{StaticResource BlockTemplate}"
                        ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ItemsSource}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </Canvas>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
